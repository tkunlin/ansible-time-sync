apt_policy_write_10periodic: true
apt_policy_disable_periodic_download: true
apt_policy_purge_unattended_upgrades: true

# Optional defaults.
# 建議正式執行時由 ./run_time_sync.sh --time-server / --time-zone 傳入。
#
# time_zone: "Asia/Taipei"
#
# chrony_time_servers:
#   - "10.88.0.23"
